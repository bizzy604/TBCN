export enum SessionStatus {
  SCHEDULED = 'scheduled',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  NO_SHOW = 'no_show',
}

export enum SessionType {
  ONE_ON_ONE = 'one_on_one',
  GROUP = 'group',
  DISCOVERY = 'discovery',
}
