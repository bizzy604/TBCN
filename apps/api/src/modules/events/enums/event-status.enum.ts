export enum EventStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed',
}

export enum EventRegistrationStatus {
  REGISTERED = 'registered',
  CANCELLED = 'cancelled',
  ATTENDED = 'attended',
}
